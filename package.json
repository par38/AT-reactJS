{
  "name": "front",
  "version": "0.1.0",
  "private": true,
  "proxy": "https://localhost:8001",
  "dependencies": {
    "@material-ui/core": "^3.9.3",
    "@material-ui/icons": "^3.0.2",
    "axios": "^0.21.1",
    "dotenv": "^8.0.0",
    "materialize-css": "^1.0.0-rc.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-router-dom": "^5.0.0",
    "react-scripts": "3.0.0",
    "serve": "^11.1.0",
    "styled-components": "^4.3.2"
  },
  "scripts": {
    "dev": "PORT=8000 react-scripts start",
    "start": "serve -s build",
    "start-with-api": "concurrently \"react-scripts start\" \"PORT=8001\"",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "heroku-postbuild": "npm run build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "concurrently": "^4.1.0"
  }
}